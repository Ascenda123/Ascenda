'use client';

import { SectionNav } from '@/components/layout/section-nav';
import { EXPLORE_SECTION_ITEMS } from '@/components/layout/navigation';

export const UniversitySearchNav = () => {
  return <SectionNav items={EXPLORE_SECTION_ITEMS} />;
};
